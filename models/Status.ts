/**
 * @enum {Status} status of the user.
 * @property {string} Active status of the user.
 * @property {string} Offline status of the user.

 */
enum Status {
    Active = "ACTIVE",
    Offline = "OFFLINE"
};

export default Status;