/**
 * @enum {Location} the location of the tuiter user
 * @property {number} latitude the latitude of the location.
 * @property {number} longitude the longitude of the location.
 */
export default interface Location {
    latitude: number;
    longitude: number;
};